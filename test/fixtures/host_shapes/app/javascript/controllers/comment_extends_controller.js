import { Controller } from "@hotwired/stimulus"
// Every subclass extends Controller through here; see docs.
import Base from "./base_form_controller"
export default class extends Base {
  pulse() {}
}
